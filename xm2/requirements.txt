# Requirements for xm2 visualization tool
numpy>=1.20.0
pandas>=1.3.0
requests>=2.25.0
flask>=2.0.0
werkzeug>=2.0.0
pyarrow>=7.0.0  # 用于pandas读取parquet文件
safetensors>=0.3.0  # 可能需要用于处理safetensors文件

# 以下是可选依赖，如果你想完全独立于lerobot运行
# 如果你已经安装了lerobot，可以注释掉这些
# datasets>=2.0.0  # 用于处理HuggingFace数据集
